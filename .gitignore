# Segredos — NUNCA versionar (premissa inegociável nº 3)
.env
.env.*
!.env.example
*.pem
*.key

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Dados brutos e processados (reproduzíveis via scripts/baixar_documentos.py +
# scripts/ingerir.py; manifesto.json com URLs/SHA-256 é versionado).
# chunks.jsonl (748 K, derivado de documentos públicos) é versionado para a demo
# no Streamlit Cloud e p/ o CI validar golden + e2e bm25 sem rodar a ingestão.
data/raw/
data/processed/*
!data/processed/chunks.jsonl

# Índices regeneráveis
data/index/
.chroma/

# Cache de embeddings e de modelos HF (container)
data/cache/
data/hf-cache/

# IDE / SO
.vscode/
.idea/
*.swp
.DS_Store
